import { CommandDispatcher, CommandHandler } from "./baseCommands"
const createSession: CommandHandler = (__, client, _) => {

}

export const configureGameCommands = (disptacher: CommandDispatcher) => {
    
}